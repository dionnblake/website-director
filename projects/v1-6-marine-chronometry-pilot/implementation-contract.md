# IMPLEMENTATION CONTRACT: KESTREL & ROWE

> **Status:** ACTIVE BINDING CONTRACT  
> **Target Directory:** `projects/v1-6-marine-chronometry-pilot/dist/`  
> **Governing Standards:** 5 Verified Locks, `DESIGN-CONSTITUTION.md` §7, `GSAP-IMPLEMENTATION-PROTOCOL.md`  

---

## Strict Prohibitions
1. No layout property animation (`transition: all`, `width`, `height`, `top`, `left` strictly banned).
2. No generic AI slop (no pill cards, no purple gradients, no fake dashboard widgets, no artificial 01/02/03 numbering).
3. No unmapped CSS values — all colors and typography must consume design system tokens.
4. Mandatory reduced motion via `(prefers-reduced-motion: reduce)`.
